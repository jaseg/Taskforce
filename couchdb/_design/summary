"language":"javascript"
"views":{
	"by_tag":{
		"map":"function(doc){
			if(doc.type == "node"){
				emit(doc.id, {"_id":doc._id, "nodetype":doc.nodetype, "owner":doc.owner, "acl":doc.acl, "summary":doc.data.summary});
				for(var i in doc.tags){
					emit(i, doc._id);
				}
			}
		}"
	}
}
